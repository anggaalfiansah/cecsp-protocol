{
  "name": "cecsp-protocol",
  "private": true,
  "type": "module",
  "scripts": {
    "install": "cd secure-server && bun install && cd ../secure-client && bun install",
    "dev": "concurrently \"cd secure-server && bun --watch server.ts\" \"cd secure-client && bun run dev --host\"",
    "build": "bun run secret && cd secure-server && bun run build && cd ../secure-client && bun run build",
    "secret": "bun ./scripts/generate-secret.ts"
  },
  "dependencies": {
    "concurrently": "^9.2.1",
    "dotenv": "^17.2.3",
    "radash": "^12.1.1",
    "typescript": "^5.9.3"
  },
  "devDependencies": {
    "@types/node": "^24.10.1"
  }
}